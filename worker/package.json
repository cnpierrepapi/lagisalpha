{
  "name": "agenthesis-worker",
  "private": true,
  "type": "module",
  "description": "EC2 live desk worker — runs the runner/engine headless on live TxLINE and mirrors to Supabase.",
  "scripts": {
    "start": "tsx desk_worker.ts"
  },
  "dependencies": {
    "tsx": "^4.19.2"
  }
}
